#extension GL_OES_EGL_image_external : require
precision mediump float;
uniform samplerExternalOES uTexture;
varying vec2 texturePos;

void main() {
    gl_FragColor= vec4(texture2D(uTexture,texturePos).rgb,1.0);
}